# Ping-Pong Frontend

Simple Angular frontend that calls a backend ping endpoint.

Quick start:

1. Install deps: `npm install`
2. Start dev server: `npm start`

Default backend base URL: `http://localhost:8000` (configurable in `src/environments/environment.ts`).
